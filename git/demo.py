# git clone-remote repo=local wp(exact copy)
# git push-local ws=remote repo(changes)
# # git pull=remote repo=local repo(changes)

# Git add -A=add all untracked files to staging area
# git commit -m "msg"=add stage file to local repo

# git init = initializing git repository in present

# git status= it will list all untracked files

